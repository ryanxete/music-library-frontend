import React, { useState } from 'react';

const SearchBar = (props) => {
    return ( 
        <div>
            <input type="text" />
            <button>search</button>
        </div>
     );
}
 
export default SearchBar;